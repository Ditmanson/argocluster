apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tech-pvc
  namespace: hugo
spec:
  storageClassName: local-storage
  accessModes: 
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi


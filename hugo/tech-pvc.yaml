apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tech-pvc
  namespace: hugo
spec:
  storageClassName: nfs-csi
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi


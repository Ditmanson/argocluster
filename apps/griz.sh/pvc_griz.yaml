apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-griz
  namespace: griz
spec:
  storageClassName: nfs-csi
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi


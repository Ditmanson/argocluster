apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: api-pvc
  namespace: griz
spec:
  storageClassName: nfs-csi
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi


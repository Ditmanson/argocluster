apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dogs-pvc
  namespace: griz
spec:
  storageClassName: nfs-csi
  accessModes: 
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

